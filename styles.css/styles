:root {
  --paragraph-color: #757575;
  --link-color: #2196f3;
  --bg-color: #2f303a;
  --writing-color: #212121;
}
body {
  font-family: Open-sans;
  font-style: Roboto;
  font-weight: 400;
  font-weight: 400;
  font-size: 36px;
  line-height: 1.66;
}
.unstyled-list {
  list-style-type: none;
}
.page-header {
  background-color: #2f303a;
}

.link {
  color: var(--writing-color);
}
.link:hover .link:focus .link:active {
  color: var(--link-color);
}
